apiVersion: v1
kind: Service
metadata:
  name: orderflow-admin-panel
  namespace: orderflow
spec:
  type: ClusterIP
  selector:
    app: orderflow-admin-panel
  ports:
    - name: http
      port: 80
      targetPort: http
